In an alternate universe, Dark Superman leads the Insurgency.

He must be stopped. 

Enter the Dark Knight. He is the hero the universe deserves. Batman is here to stop the dark kryptonian before this alternate universe is turned on it's head.

Begin your fight.

~~~~~~~~~~~~~~~~~~~~~~~~

function batmanAttacks(getBatmanAttack){
    let attackRoll = rollDie(20);

    let damage = 0;

    // Weak Attack

    if(getBatmanAttack == 1){
        if(attackRoll > 5){
            console.log("Batman weak attack hits!");
            damage = ;
        }
        else{
            console.log("Batman weak attack misses.");
        }
    }

    // Strong attack

    else if(attackChoice == 2){
        if(attackRoll > 10){
            console.log("Batman strong attack hits!");
            damage = ;
        }
        else{
            console.log("Batman strong attack misses.");
        }
    }
    else{
        console.log("Attack was not valid. Batman deals no damage.");
    }

    return damage;


function supermanAttacks(){
	let attackRoll = rollDie(20);

	let damageRoll = 0;	
	if(attackRoll >= 15)
	{
		damageRoll = rollDie(12);
		console.log("Superman hits for " + damageRoll + " damage!");
	}
	else if (attackRoll >= 10)
	{
		damageRoll = rollDie(10);
		console.log("Superman hits for " + damageRoll + " damage!");
	}
	else
	{
		console.log("Superman misses...");
	}
	return damageRoll;

